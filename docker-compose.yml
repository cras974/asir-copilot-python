version: '3.8'

services:
  backend:
    build: ./backend
    container_name: asir-copilot-backend
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    volumes:
      - ./frontend:/frontend
    restart: unless-stopped